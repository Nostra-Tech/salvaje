// V6 Ajuste 31 — Vitest setup placeholder.
// Run `npm install -D vitest @testing-library/react @testing-library/jest-dom jsdom`
// before `npm run test`.
//
// Add `test:` config to vite.config.js when expanding the suite:
//   import { defineConfig } from 'vitest/config'
//   test: { globals: true, environment: 'jsdom', setupFiles: ['./src/test/setup.js'] }
